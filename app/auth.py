from .models import User

class AuthService:
    def __init__(self, username, password):
        # self.user = UserController.find(username, password)
        pass

    def login(self):
        """Create new session and login user to app"""
        

    def changePassword(self):
        pass